export const WEATHER_API_ACTION = {
    affects: 'affects',
    all: ':all',
    id: ':id',
    closest: 'closest',
    contains: 'contains',
    route: 'route',
    search: 'search',
    within: 'within'
} as const;
