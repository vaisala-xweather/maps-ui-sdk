export const DEFAULT_COLLISION_PADDING = 0;

export const POSITION = {
    absolute: 'absolute',
    relative: 'relative',
    fixed: 'fixed',
    sticky: 'sticky',
    static: 'static'
} as const;

export const SIDE = {
    top: 'top',
    right: 'right',
    bottom: 'bottom',
    left: 'left'
} as const;

export const ALIGN = {
    start: 'start',
    center: 'center',
    end: 'end'
} as const;
